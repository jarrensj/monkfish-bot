# Telegram & backend
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

SOLANA_RPC=https://api.devnet-beta.solana.com
# WALLET_MODE will matter when Privy adapter is added
WALLET_MODE=local

KEYVALUE_DIR=.data


# Backend base URL + API key (if any)
KOI_API_URL=https://koi.example.com
KOI_API_KEY=# optional bearer key

# Adapters
WALLET_MODE=development    # local | http   (http = use backend for wallet ops/Privy)
SWAP_PROVIDER=development   # mock  | http   (http = use backend for quotes/swaps)

# Optional HTTP client timeout (ms)
HTTP_TIMEOUT_MS=8000
